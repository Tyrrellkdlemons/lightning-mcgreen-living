/**
 * DemoBanner — kept as a stub so any old import resolves, but the component
 * now renders nothing.
 *
 * The honest "what's real / what's sample / what's missing" breakdown lives
 * at `docs/DATA_PROVENANCE.md`. The compliance guard `publishable<T>()` in
 * `src/lib/compliance/guards.ts` still filters listings missing full source
 * metadata in production mode.
 */
export function DemoBanner() {
  return null;
}
